## AKTU Academic Autonomy Portal – Docs

- **Workflow**: See `final_AKTU_Autonomy_WebApp_Workflow_Approval.md` in the repo root for the end-to-end status pipeline and roles.
- **API Contract**: The first implemented endpoint is `GET /api/health` returning `{"status":"ok","service":"aktu-autonomy-portal"}`.

As more modules are added (applications, scrutiny, committees, UGC, etc.), document their request/response contracts here.

